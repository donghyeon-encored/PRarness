{
  "name": "prarness",
  "version": "1.0.0",
  "description": "The deterministic core of an issue-to-PR agent pipeline.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "npm run lint --prefix .github/agent-pipeline",
    "test": "npm test --prefix .github/agent-pipeline"
  }
}
