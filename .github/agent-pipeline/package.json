{
  "name": "issue-review-agent-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node --check pipeline.mjs && node --check cloud-bridge.mjs && node --check repository-check.mjs && node --check cloud-github.mjs && node --check cloud-publish.mjs && node --check github-app-controller.mjs && node --check controller-dispatch.mjs && bash -n cloud-github-setup.sh && bash -n cloud-environment-bootstrap.sh",
    "test": "node --test test/*.test.mjs"
  }
}
