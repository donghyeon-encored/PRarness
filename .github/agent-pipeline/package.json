{
  "name": "issue-review-agent-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node --check pipeline.mjs && node --check repository-check.mjs && node --check hostless-intake.mjs && node --check cloud-contract.mjs && node --check cloud-analysis.mjs && node --check cloud-github.mjs && node --check cloud-publish.mjs && node --check cloud-session.mjs && bash -n cloud-github-setup.sh && bash -n cloud-environment-bootstrap.sh",
    "test": "node --test test/*.test.mjs"
  }
}
